---

name: Build and Push Docker Image (Docker Hub + GHCR)

env:
  HASKELL_VERSION: ${{ vars.HASKELL_VERSION }}

on:
  push:
    branches: ["master"]
    tags-ignore: ["*"]

jobs:

  docker:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - name: checkout
        uses: actions/checkout@v4

      - name: set up qemu
        uses: docker/setup-qemu-action@v3

      - name: set up docker buildx
        uses: docker/setup-buildx-action@v3

      - name: login to dockerhub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: log in to github container registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: build and push
        uses: docker/build-push-action@v5
        with:
          push: true
          build-args: |
            HASKELL_VERSION=${{ env.HASKELL_VERSION }}
          tags: |
            frankhjung/haskell:${{ env.HASKELL_VERSION }}
            frankhjung/haskell:latest
            ghcr.io/frankhjung/haskell:${{ env.HASKELL_VERSION }}
            ghcr.io/frankhjung/haskell:latest
